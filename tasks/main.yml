---
- name: Copy tftp files
  copy:
    src: files/tftp/{{ item }}
    dest: /var/lib/tftpboot/
    owner: root
    group: root
    mode: '0644'
  loop:
    - undionly.kpxe
    - bootstrap.ipxe
    - ipxe.efi

- name: Enable razor server
  systemd:
        name: razor-server
        enabled: true
